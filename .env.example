# Optional host-port overrides for the local Docker environment.
CONFIGOPS_WP_PORT=8888
CONFIGOPS_DB_PORT=3307
CONFIGOPS_MAILPIT_PORT=8025
CONFIGOPS_SMTP_PORT=1025

# Use "off" to disable Xdebug completely. The default waits for an IDE trigger.
XDEBUG_MODE=develop,debug
